# 2026-08-23 CI 自检证据

## 1. 验证目标

依据 `tasks.md`：任务合入 dev 后核对 `.github/workflows/ci.yaml` 的 ubuntu 构建日志（该 CI 完整执行 clone+docx 转换+vitepress+Nuxt 冒烟，`NODE_OPTIONS` 为 5120MB），记录构建时长、内存表现与 `图片处理失败` 条目数量。

## 2. 触发信息

- 工作流：`文档站生产构建自检`（ci.yaml），run ID `32613873322`
- 触发：`dev` 分支 push 提交 `8355a32`（📃 docs(prompts): build-by-node-in-vercel 提示词移除 x-emf 不处理清单）
- Job：`生产构建链路自检`，状态 **completed / success**
- 执行时间：2026-08-23 02:51:09Z → 02:58:36Z，**7 分 27 秒**（历史同工作流约 6m58s，增长约 30s，属可接受范围）

## 3. 关键日志证据

```log
[info] EMF/WMF 转换统计: 成功 403 张，失败 0 张
```

|                     核对项                      |                                                             结果                                                              |                 结论                 |
| :---------------------------------------------: | :---------------------------------------------------------------------------------------------------------------------------: | :----------------------------------: |
|          EMF/WMF 转换统计（成功/失败）          |                                                       成功 403，失败 0                                                        |              ✓ 全量成功              |
|              `图片处理失败` 条目数              |                                                               0                                                               |             ✓ 零失败回退             |
|                  EMF 总量口径                   |                 GitHub 仓库 drill-docx 全量 403 个 EMF（本地手工副本抽样 382 个，CI 克隆仓库为 403 个，更全）                 |           ✓ 容器内全量覆盖           |
|                    构建内存                     |                                     5120MB `NODE_OPTIONS` 下构建成功，job success，无 OOM                                     |                  ✓                   |
|                    构建时长                     |                                              7 分 27 秒（历史 6 分 58 秒 +29s）                                               |               ✓ 可接受               |
|          `Ignored build scripts` 警告           | `simple-git-hooks`、`vue-demi`（**既有依赖**，非本次新增的 emf-converter/@napi-rs/canvas——后者 registry 实证无 install 脚本） |            ✓ 无需补白名单            |
| `[emf-converter] Unhandled EMR record type: 90` |                                     库对记录类型 90 跳过并继续渲染，转换仍成功（403/403）                                     | ⚠ 库已知局限，记录在案；不影响成功率 |

## 4. 结论

- CI 自检通过：ubuntu 容器内完整构建（clone 真实仓库 drill-docx → 403 EMF 全量转换零失败 → vitepress+Nuxt 冒烟）成功；
- 回归安全：与本地管线（382/382）结论一致且覆盖面更全（403/403）；
- 无新增依赖安装拦截；内存表现健康。

## 5. 遗留

- `Unhandled EMR record type: 90` 警告为 emf-converter 库对个别记录类型的跳过程序（转换结果仍非空白，目视验证阶段抽查确认质量）；若后续发现该类样本渲染质量不合格，可作为库升级或追加记录的追踪项。
