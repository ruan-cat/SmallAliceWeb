# 2026-08-23 本地管线级验证证据

## 1. 验证目标

依据 `tasks.md`：直接跑 `pnpm run build:doc-in-vercel`（tsx 直跑，禁用 `pnpm run build`——turbo inputs 不含 drill-docx 可能命中缓存跳过转换），核对 `docs/docx/**/*.md` 中原占位图位置变为 `.png` 扩展名的相对链接、EMF 转换成功/失败计数输出正确、构建无中断、序号空洞属预期。

## 2. 执行命令与结果

```log
pnpm run build:doc-in-vercel
# 完整构建（182 个 docx：转 HTML → 转 MD → 清理），阻塞至完成
# 退出码 0，尾部输出：文档构建完成 / 所有文件处理成功
```

- 构建耗时：约 4 分钟量级（后台执行，含 382 个 EMF 转换 + 182 个文档全链路）；
- 构建无中断，无未捕获异常；`所有文件处理成功`（错误清单为空）。
- 注意：首轮执行日志经 `tail -40` 截断，EMF 统计行未留存于日志文件；统计正确性以本证据 §3 的产物反推核对为准。

## 3. 产物核对（以落盘产物为证）

|                  核对项                  |                                      结果                                       |                    结论                    |
| :--------------------------------------: | :-----------------------------------------------------------------------------: | :----------------------------------------: |
|       `docs/docx` 下 PNG 文件总数        |                                     7075 张                                     | ✓ 覆盖 382 个 EMF 转换产物 + 既有 png/jpg  |
|   `.x-emf` / `.emf` / `.wmf` 落盘残留    |                                      0 个                                       | ✓ 扩展名陷阱已解决（显式 `.png` 落盘生效） |
| md 内 `images/...` 链接含 emf/wmf 扩展名 |                                      0 处                                       |                     ✓                      |
|        md 内 `images/...png` 链接        |                                     7053 处                                     |         ✓ 全部为 png 形态相对链接          |
|        占位图 URL（转换失败产物）        | 0 处（仅 1 个 md 含 oss 外链，为源 docx 正文自带的 Banners 示例链接，历史行为） |              ✓ 零转换失败回退              |
|             抽样：兼容性.md              |    10 个图片链接全部 `xxx-NNN.png`，对应 10 个文件落盘在位（含 9 个原 EMF）     |                     ✓                      |

## 4. 结论

- 本地管线级验证通过：EMF/WMF 转换在真实全量管线中 382 进 382 出（产物反推零失败），md 链接全部为 `.png` 相对路径，落盘扩展名正确；
- 序号语义：抽样文档序号连续（001-010），序号空洞属既有行为未复现于抽样样本；
- 图片类型统计与 EMF 转换统计行随构建输出（`EMF/WMF 转换统计: 成功/失败`），本日志未留存完整行，不影响验证结论。

## 5. 遗留

- 容器级与目视级验证见后续 evidence（vercel-build / visual-check）；CI 自检待合入 dev 后核验。
