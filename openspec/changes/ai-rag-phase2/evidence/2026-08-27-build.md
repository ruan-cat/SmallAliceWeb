# 2026-08-27 完整文档构建证据

## 1. 验证命令

```log
$env:NODE_OPTIONS='--max-old-space-size=8192'; pnpm run docs:build
```

构建采用仓库既定的 `turbo run //#docs:build:run --concurrency=1` 串行策略。

## 2. 结果

|    项目    |                      结果                       |
| :--------: | :---------------------------------------------: |
|   根命令   |              `pnpm run docs:build`              |
|  Node 堆   |           `--max-old-space-size=8192`           |
| Turbo 任务 |             9 successful / 9 total              |
| VitePress  | client + server bundles、pages rendering 均完成 |
|   总耗时   |                    4m45.894s                    |
|   退出码   |                        0                        |

`ai-vue-doc` 的 Nuxt 子构建同时完成，prerender 阶段显示 `Prerendered 0 routes`，不再请求易触发 h3 版本冲突的内容 API。

## 3. 根因与修复边界

首次 fresh build 在 `@ztl-uwu/nuxt-content` 的内部内容 API prerender 处报 `Invalid URL`，排除 `/api/_content/` 前缀后该错误消失；随后 `nuxt-og-image@5.1.13` 与当前 Nuxt 3/Nitro h3 1.x 运行时产生 `sendError` 缺失导入，项目未使用 OG Image，因此配置 `ogImage.enabled=false`。两项均位于 `packages/ai-vue-doc/nuxt.config.ts`，未修改依赖版本或运行时 API。

## 4. 当前边界

本证据只证明本地完整构建成功；Git 集成 Production deployment 仍需以最新 `main` checkout SHA 的 Vercel `Ready` 状态和生产浏览器回归作为 2.2.4 最终证据。
