@ruan-cat:registry=https://registry.npmjs.org/

# 禁止 pnpm 自动安装缺失的 peer 依赖（防止 vite-plugin-checker 的 optional peer @biomejs/biome 被自动拉回）
auto-install-peers=false

# 禁止 pnpm 自动安装缺失的 peer 依赖（防止 vite-plugin-checker 的 optional peer @biomejs/biome 被自动拉回）
auto-install-peers=false
